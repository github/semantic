package main

func main() {
type x y
type (
  x y
  e f
)
type (
// Read(cnt) == (wantStr, wantErr)
testWrite struct {
	cnt     int
	wantStr string
	wantErr error
}
testFac interface{} // testRead | testWriteTo | testRemaining
)
}
